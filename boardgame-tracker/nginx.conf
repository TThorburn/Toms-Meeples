server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Docker internal DNS resolver — required so nginx can find
    # other containers by name at request time (not just at startup)
    resolver 127.0.0.11 valid=10s ipv6=off;

    # ── Health check endpoint ────────────────────────────────────
    location /healthz {
        access_log off;
        return 200 "ok\n";
        add_header Content-Type text/plain;
    }

    # ── API proxy → bg-api container ────────────────────────────
    # Using a variable forces nginx to resolve the hostname at
    # request time rather than at startup (prevents crash if
    # bg-api isn't up yet).
    location /api/ {
        set $backend http://bg-api:3001;
        proxy_pass $backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Connection "";

        # BGG scraping can be slow — give it plenty of time
        proxy_read_timeout 120s;
        proxy_connect_timeout 10s;
        proxy_send_timeout 120s;
    }

    # ── Static assets with cache headers ────────────────────────
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }

    # ── React SPA — all other routes fall back to index.html ────
    location / {
        try_files $uri $uri/ /index.html;
    }
}
